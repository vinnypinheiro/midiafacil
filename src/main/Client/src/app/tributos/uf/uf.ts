import {DomainBase} from '../../utils/utils'; 

export interface Uf extends DomainBase { 

     uf_codigo: string; 
     trb_pais_id_id :  number; 
     trb_pais_id_nome :  string; 
     trb_pais_id_codigo :  string; 
     uf_nome: string; 
}