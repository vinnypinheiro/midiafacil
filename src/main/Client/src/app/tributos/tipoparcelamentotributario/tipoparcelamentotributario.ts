import {DomainBase} from '../../utils/utils'; 

export interface TipoParcelamentoTributario extends DomainBase { 

     tipoparcelamentotributario_codigo: string; 
     tipoparcelamentotributario_nome: string; 
}