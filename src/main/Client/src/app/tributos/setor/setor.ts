import {DomainBase} from '../../utils/utils'; 

export interface Setor extends DomainBase { 

     trb_distrito_id_id :  number; 
     trb_distrito_id_nome :  string; 
     trb_distrito_id_codigo :  string; 
     setor_codigo: string; 
     setor_nome: string; 
}