import {DomainBase} from '../../utils/utils'; 

export interface CnaeDivisao extends DomainBase { 

     trb_cnaesecao_id_id :  number; 
     trb_cnaesecao_id_nome :  string; 
     trb_cnaesecao_id_codigo :  string; 
     cnaedivisao_codigo: string; 
     cnaedivisao_nome: string; 
}