import {DomainBase} from '../../utils/utils'; 

export interface CnaeGrupo extends DomainBase { 

     trb_cnaedivisao_id_id :  number; 
     trb_cnaedivisao_id_nome :  string; 
     trb_cnaedivisao_id_codigo :  string; 
     cnaegrupo_codigo: string; 
     cnaegrupo_nome: string; 
}